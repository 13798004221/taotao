<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taotao.admin.mapper.TestMapper">
	
	<!-- 查询数据库当前时间 -->
	<select id="queryDate" resultType="string">
		select now()
	</select>
</mapper>
